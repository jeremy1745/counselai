DATABASE_URL=postgresql+asyncpg://counselai:counselai@postgres:5432/counselai
QDRANT_URL=http://qdrant:6333
OLLAMA_URL=http://ollama:11434
UPLOAD_DIR=/app/data/uploads

# Required — must be at least 32 characters. Generate with: openssl rand -hex 32
SECRET_KEY=

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000

# JWT token expiry in minutes (default: 480 = 8 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Max file upload size in MB (default: 300)
MAX_UPLOAD_SIZE_MB=300

# Account lockout: max failed login attempts before locking (default: 5)
MAX_FAILED_LOGINS=5

# Account lockout: how long to lock in minutes (default: 15)
LOCKOUT_DURATION_MINUTES=15
